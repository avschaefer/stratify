<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= company_name %> - <%= company_tagline %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      min-height: 100vh;
      background-color: #0a0a0a;
      color: #fafafa;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    }
    
    /* Backdrop gradients */
    .backdrop-gradients {
      pointer-events: none;
      position: fixed;
      inset: 0;
      z-index: -1;
    }
    
    .gradient-1 {
      position: absolute;
      inset-x: 0;
      top: -20%;
      margin: 0 auto;
      height: 500px;
      width: 900px;
      border-radius: 9999px;
      background: linear-gradient(to bottom, rgba(99, 102, 241, 0.2), transparent);
      filter: blur(64px);
    }
    
    .gradient-2 {
      position: absolute;
      right: -10%;
      top: 30%;
      height: 400px;
      width: 600px;
      border-radius: 9999px;
      background: linear-gradient(to top right, rgba(168, 85, 247, 0.2), transparent);
      filter: blur(64px);
    }
    
    /* Header */
    header {
      position: sticky;
      top: 0;
      z-index: 50;
      backdrop-filter: blur(8px);
      background-color: rgba(10, 10, 10, 0.4);
    }
    
    nav {
      max-width: 1280px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1rem;
    }
    
    .logo {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      text-decoration: none;
      color: inherit;
    }
    
    .logo-icon {
      height: 24px;
      width: 24px;
      border-radius: 0.5rem;
      background-color: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.15);
      transition: transform 0.2s;
    }
    
    .logo:hover .logo-icon {
      transform: scale(1.05);
    }
    
    .logo-text {
      font-weight: 600;
      letter-spacing: -0.025em;
    }
    
    .nav-links {
      display: none;
      align-items: center;
      gap: 0.75rem;
    }
    
    @media (min-width: 640px) {
      .nav-links {
        display: flex;
      }
    }
    
    .nav-link {
      border-radius: 0.75rem;
      padding: 0.5rem 1rem;
      font-size: 0.875rem;
      font-weight: 500;
      text-decoration: none;
      color: #e5e5e5;
      border: 1px solid rgba(255, 255, 255, 0.15);
      transition: background-color 0.2s;
    }
    
    .nav-link:hover {
      background-color: rgba(255, 255, 255, 0.05);
    }
    
    .nav-link-primary {
      background-color: white;
      color: #171717;
      font-weight: 600;
    }
    
    .nav-link-primary:hover {
      background-color: #e5e5e5;
    }
    
    .mobile-menu-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 0.5rem;
      padding: 0.5rem;
      border: 1px solid rgba(255, 255, 255, 0.15);
      color: #e5e5e5;
      background: none;
      cursor: pointer;
      transition: background-color 0.2s;
    }
    
    .mobile-menu-btn:hover {
      background-color: rgba(255, 255, 255, 0.05);
    }
    
    @media (min-width: 640px) {
      .mobile-menu-btn {
        display: none;
      }
    }
    
    .mobile-menu {
      display: none;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      background-color: rgba(10, 10, 10, 0.7);
      backdrop-filter: blur(8px);
    }
    
    .mobile-menu.open {
      display: block;
    }
    
    .mobile-menu-content {
      max-width: 1280px;
      margin: 0 auto;
      padding: 1rem;
    }
    
    .mobile-menu-links {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
    }
    
    /* Hero */
    .hero {
      position: relative;
    }
    
    .grid-pattern {
      pointer-events: none;
      position: absolute;
      inset: 0;
      z-index: -1;
      background-image: 
        linear-gradient(to right, rgba(255, 255, 255, 0.04) 1px, transparent 1px),
        linear-gradient(to bottom, rgba(255, 255, 255, 0.04) 1px, transparent 1px);
      background-size: 36px 36px;
    }
    
    .hero-content {
      max-width: 1280px;
      margin: 0 auto;
      padding: 5rem 1rem;
      text-align: center;
    }
    
    @media (min-width: 640px) {
      .hero-content {
        padding: 7rem 1rem;
      }
    }
    
    .hero-title {
      font-size: 2.25rem;
      font-weight: 600;
      letter-spacing: -0.025em;
      line-height: 1.2;
      max-width: 48rem;
      margin: 0 auto;
    }
    
    @media (min-width: 640px) {
      .hero-title {
        font-size: 3rem;
      }
    }
    
    @media (min-width: 768px) {
      .hero-title {
        font-size: 3.75rem;
      }
    }
    
    .hero-description {
      margin-top: 1.25rem;
      color: #d4d4d4;
      font-size: 1rem;
      max-width: 32rem;
      margin-left: auto;
      margin-right: auto;
    }
    
    @media (min-width: 640px) {
      .hero-description {
        font-size: 1.125rem;
      }
    }
    
    .hero-cta {
      margin-top: 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.75rem;
      flex-wrap: wrap;
    }
    
    .btn-primary {
      border-radius: 1rem;
      background-color: white;
      padding: 0.75rem 1.25rem;
      font-weight: 600;
      color: #171717;
      text-decoration: none;
      box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
      transition: background-color 0.2s;
    }
    
    .btn-primary:hover {
      background-color: #e5e5e5;
    }
    
    .btn-secondary {
      border-radius: 1rem;
      padding: 0.75rem 1.25rem;
      font-weight: 500;
      color: #fafafa;
      text-decoration: none;
      border: 1px solid rgba(255, 255, 255, 0.2);
      transition: background-color 0.2s;
    }
    
    .btn-secondary:hover {
      background-color: rgba(255, 255, 255, 0.05);
    }
    
    /* Features */
    .features-section {
      position: relative;
    }
    
    .features-content {
      max-width: 1280px;
      margin: 0 auto;
      padding: 0 1rem 5rem;
    }
    
    .features-header {
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    
    .features-title {
      font-size: 1.25rem;
      font-weight: 600;
      color: #e5e5e5;
    }
    
    .features-link {
      display: none;
      font-size: 0.875rem;
      color: #a3a3a3;
      text-decoration: none;
      transition: color 0.2s;
    }
    
    .features-link:hover {
      color: #e5e5e5;
    }
    
    @media (min-width: 640px) {
      .features-link {
        display: inline;
      }
    }
    
    .features-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    
    @media (min-width: 640px) {
      .features-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    
    @media (min-width: 1024px) {
      .features-grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    
    .feature-card {
      position: relative;
      overflow: hidden;
      border-radius: 1rem;
      border: 1px solid rgba(255, 255, 255, 0.1);
      background-color: rgba(255, 255, 255, 0.02);
      padding: 1.25rem;
      transition: background-color 0.2s;
    }
    
    .feature-card:hover {
      background-color: rgba(255, 255, 255, 0.04);
    }
    
    .feature-glow {
      position: absolute;
      right: -2.5rem;
      top: -2.5rem;
      height: 8rem;
      width: 8rem;
      border-radius: 9999px;
      background: linear-gradient(to bottom right, rgba(255, 255, 255, 0.1), transparent);
      filter: blur(32px);
      transition: opacity 0.2s;
    }
    
    .feature-card:hover .feature-glow {
      opacity: 0.6;
    }
    
    .feature-icon-wrapper {
      margin-bottom: 1rem;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 0.75rem;
      background-color: rgba(255, 255, 255, 0.1);
      padding: 0.75rem;
      border: 1px solid rgba(255, 255, 255, 0.15);
    }
    
    .feature-icon {
      color: #fafafa;
      height: 24px;
      width: 24px;
    }
    
    .feature-title {
      font-size: 1.125rem;
      font-weight: 600;
      letter-spacing: -0.025em;
    }
    
    .feature-body {
      margin-top: 0.5rem;
      font-size: 0.875rem;
      line-height: 1.625;
      color: #d4d4d4;
    }
    
    
    /* Footer */
    footer {
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .footer-content {
      max-width: 1280px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 2.5rem 1rem;
      font-size: 0.875rem;
      color: #a3a3a3;
    }
    
    @media (min-width: 640px) {
      .footer-content {
        flex-direction: row;
      }
    }
    
    .footer-links {
      display: flex;
      align-items: center;
      gap: 1.5rem;
    }
    
    .footer-link {
      color: #a3a3a3;
      text-decoration: none;
      transition: color 0.2s;
    }
    
    .footer-link:hover {
      color: #e5e5e5;
    }
    
    .hidden {
      display: none;
    }
  </style>
</head>
<body>
  <!-- Backdrop gradients -->
  <div class="backdrop-gradients" aria-hidden="true">
    <div class="gradient-1"></div>
    <div class="gradient-2"></div>
  </div>

  <!-- NAV -->
  <header>
    <nav>
      <a href="#" class="logo">
        <div class="logo-icon"></div>
        <span class="logo-text"><%= company_name %></span>
      </a>
      <div class="nav-links">
        <%= link_to "Login", login_path, class: "nav-link" %>
        <%= link_to "Get started", signup_path, class: "nav-link nav-link-primary" %>
      </div>
      <button class="mobile-menu-btn" onclick="toggleMobileMenu()" aria-label="Toggle menu">
        <svg viewBox="0 0 24 24" style="height: 20px; width: 20px;" fill="none" stroke="currentColor" stroke-width="1.6">
          <path id="menu-icon" stroke-linecap="round" d="M4 7h16M4 12h16M4 17h16" />
        </svg>
      </button>
    </nav>
    <!-- Mobile sheet -->
    <div class="mobile-menu" id="mobile-menu">
      <div class="mobile-menu-content">
        <div class="mobile-menu-links">
          <%= link_to "Login", login_path, class: "nav-link" %>
          <%= link_to "Get started", signup_path, class: "nav-link nav-link-primary" %>
        </div>
      </div>
    </div>
  </header>

  <!-- HERO -->
  <section class="hero">
    <div class="grid-pattern" aria-hidden="true"></div>
    <div class="hero-content">
            <h1 class="hero-title"><%= company_tagline %></h1>
            <p class="hero-description">
              <%= company_description %>
            </p>
      <div class="hero-cta">
        <%= link_to "Get started", signup_path, class: "btn-primary", id: "get-started" %>
        <%= link_to "Login", login_path, class: "btn-secondary", id: "login" %>
      </div>
    </div>
  </section>

  <!-- FEATURES -->
  <section class="features-section" id="features">
    <div class="features-content">
      <div class="features-header">
        <h2 class="features-title">Everything you need</h2>
        <a href="#" class="features-link">See how it works →</a>
      </div>
      <div class="features-grid">
        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" d="M4 19h16M5 15l3-4 3 3 6-8" />
            </svg>
          </div>
          <h3 class="feature-title">Portfolio Tracking</h3>
          <p class="feature-body">Monitor your investments with concise analytics and performance tracking.</p>
        </article>

        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <path stroke="currentColor" stroke-linecap="round" d="M3 10a7 7 0 0111.95-4.95L19 6m2 4v6a3 3 0 01-3 3H6a3 3 0 01-3-3v-6h18z" />
              <path stroke="currentColor" stroke-linecap="round" d="M12 12v4m0-6h.01" />
            </svg>
          </div>
          <h3 class="feature-title">Savings & Expenses</h3>
          <p class="feature-body">Track savings accounts and expenses with monthly snapshots and trend analysis.</p>
        </article>

        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <rect x="3" y="4" width="18" height="14" rx="3" stroke="currentColor" />
              <path stroke="currentColor" d="M7 8h10M7 12h6" />
            </svg>
          </div>
          <h3 class="feature-title">Retirement Planning</h3>
          <p class="feature-body">Model scenarios and goals to stay on track for the future you want.</p>
        </article>

        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <path stroke="currentColor" d="M4 7h16M6 11h12M8 15h8M10 19h4" />
              <rect x="3" y="4" width="18" height="16" rx="3" stroke="currentColor" />
            </svg>
          </div>
          <h3 class="feature-title">Loan Management</h3>
          <p class="feature-body">Calculate payments, view amortization, and optimize your debt strategy.</p>
        </article>

        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <path stroke="currentColor" d="M12 3l8 4v5c0 5-3.5 8.5-8 9-4.5-.5-8-4-8-9V7l8-4z" />
              <path stroke="currentColor" d="M9.5 12.5l1.8 1.8 3.7-3.8" />
            </svg>
          </div>
          <h3 class="feature-title">Insurance Management</h3>
          <p class="feature-body">Organize policies, coverage amounts, and premium schedules in one place.</p>
        </article>

        <article class="feature-card">
          <div class="feature-glow"></div>
          <div class="feature-icon-wrapper">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="1.6" class="feature-icon">
              <rect x="3" y="4" width="18" height="16" rx="3" stroke="currentColor" />
              <path stroke="currentColor" d="M7 9h10M7 13h6M7 17h4" />
            </svg>
          </div>
          <h3 class="feature-title">Tax Planning</h3>
          <p class="feature-body">Estimate taxes, plan deductions, and optimize your strategy with modeling.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <div class="footer-content">
      <p>© <%= Date.current.year %> <%= company_name %></p>
      <div class="footer-links">
        <a href="#" class="footer-link">Privacy</a>
        <a href="#" class="footer-link">Terms</a>
        <a href="#" class="footer-link">Contact</a>
      </div>
    </div>
  </footer>

  <script>
    function toggleMobileMenu() {
      const menu = document.getElementById('mobile-menu');
      const icon = document.getElementById('menu-icon');
      const isOpen = menu.classList.contains('open');
      
      if (isOpen) {
        menu.classList.remove('open');
        document.body.style.overflow = 'auto';
        icon.setAttribute('d', 'M4 7h16M4 12h16M4 17h16');
      } else {
        menu.classList.add('open');
        document.body.style.overflow = 'hidden';
        icon.setAttribute('d', 'M6 6l12 12M18 6L6 18');
      }
    }
  </script>
</body>
</html>
