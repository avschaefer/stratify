<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Stratify</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Tailwind CSS (using CDN for simplicity as requested in prompt context, though importmap/css bundling is better for prod) -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Charts.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style type="text/tailwindcss">
      body {
        font-family: 'Inter', sans-serif;
      }
      
      .animate-fade-in {
        animation: fadeIn 0.5s ease-out forwards;
      }
      
      @keyframes fadeIn {
        from { opacity: 0; transform: translateY(10px); }
        to { opacity: 1; transform: translateY(0); }
      }

      /* Scrollbar Styling */
      ::-webkit-scrollbar {
        width: 8px;
        height: 8px;
      }
      ::-webkit-scrollbar-track {
        background: transparent; 
      }
      ::-webkit-scrollbar-thumb {
        background: #cbd5e1; 
        border-radius: 4px;
      }
      ::-webkit-scrollbar-thumb:hover {
        background: #94a3b8; 
      }

      /* UI Standardization */
      @layer components {
        .btn-primary {
          @apply bg-black text-white px-4 py-2 rounded-full font-medium text-sm hover:bg-gray-900 transition-colors border border-transparent flex items-center justify-center;
        }
        .btn-secondary {
          @apply bg-white border border-gray-200 text-gray-900 px-4 py-2 rounded-full font-medium text-sm hover:bg-gray-50 transition-colors flex items-center justify-center;
        }
        .btn-danger {
          @apply bg-red-50 text-red-600 border border-red-100 px-4 py-2 rounded-full font-medium text-sm hover:bg-red-100 transition-colors flex items-center justify-center;
        }
        .input-standard {
          @apply w-full p-2 border border-gray-200 rounded-lg text-sm focus:ring-1 focus:ring-black focus:border-black bg-white outline-none transition-colors;
        }
      }
    </style>
  </head>
  
  <body class="bg-gray-50 text-gray-900 font-sans selection:bg-blue-100 min-h-screen">
    
    <!-- Mobile Header -->
    <div class="lg:hidden bg-white border-b border-gray-200 p-4 flex items-center justify-between sticky top-0 z-20">
      <div class="flex items-center gap-2">
        <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold">S</div>
        <span class="font-bold text-xl tracking-tight">Stratify</span>
      </div>
      <button class="text-gray-500 p-2">
        Menu
      </button>
    </div>

    <div class="flex max-w-8xl mx-auto">
      <!-- Sidebar -->
      <% if user_signed_in? && !['sessions', 'registrations', 'landing'].include?(controller_name) %>
        <%= render 'shared/sidebar' %>
      <% end %>

      <!-- Main Content -->
      <main class="flex-1 min-w-0 <%= 'lg:ml-64' if user_signed_in? && !['sessions', 'registrations', 'landing'].include?(controller_name) %>">
        
        <% if user_signed_in? && !['sessions', 'registrations', 'landing'].include?(controller_name) %>
        <!-- Header -->
        <header class="bg-white/80 backdrop-blur-xl sticky top-0 z-10 border-b border-gray-200 px-8 py-4 flex justify-between items-center h-20">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">
              <%= 
                case controller_name
                when 'dashboard' then 'Dashboard'
                when 'portfolios' then 'Portfolio'
                when 'accounts' then 'Savings & Expenses'
                when 'loans' then 'Loans'
                when 'insurance_policies' then 'Insurance'
                when 'taxes' then 'Taxes'
                when 'retirements' then 'Retirement'
                when 'settings' then 'Settings'
                else controller_name.humanize.titleize
                end
              %>
            </h1>
            <p class="text-sm text-gray-500">Updated just now</p>
          </div>
        </header>
        <% end %>

        <div class="p-4 md:p-8 max-w-7xl mx-auto">
          
          <% if notice %>
            <div class="mb-4 p-4 rounded-xl bg-emerald-50 text-emerald-700 border border-emerald-100 flex items-center" role="alert">
              <%= icon('shield_check', size: 20, class: "mr-2") %>
              <%= notice %>
            </div>
          <% end %>
          
          <% if alert %>
            <div class="mb-4 p-4 rounded-xl bg-red-50 text-red-700 border border-red-100 flex items-center" role="alert">
               <%= icon('shield_check', size: 20, class: "mr-2") %>
              <%= alert %>
            </div>
          <% end %>
          
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
