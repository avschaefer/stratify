<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= company_name %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
    <script src="https://unpkg.com/lightweight-charts/dist/lightweight-charts.standalone.production.js"></script>
    
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        min-height: 100vh;
        background-color: #0a0a0a;
        color: #fafafa;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      }
      
      /* Backdrop gradients */
      .backdrop-gradients {
        pointer-events: none;
        position: fixed;
        inset: 0;
        z-index: -1;
      }
      
      .gradient-1 {
        position: absolute;
        inset-x: 0;
        top: -20%;
        margin: 0 auto;
        height: 500px;
        width: 900px;
        border-radius: 9999px;
        background: linear-gradient(to bottom, rgba(99, 102, 241, 0.2), transparent);
        filter: blur(64px);
      }
      
      .gradient-2 {
        position: absolute;
        right: -10%;
        top: 30%;
        height: 400px;
        width: 600px;
        border-radius: 9999px;
        background: linear-gradient(to top right, rgba(168, 85, 247, 0.2), transparent);
        filter: blur(64px);
      }
      
      /* Grid pattern */
      .grid-pattern {
        pointer-events: none;
        position: fixed;
        inset: 0;
        z-index: 0;
        background-image: 
          linear-gradient(to right, rgba(255, 255, 255, 0.04) 1px, transparent 1px),
          linear-gradient(to bottom, rgba(255, 255, 255, 0.04) 1px, transparent 1px);
        background-size: 36px 36px;
      }
      
      /* Sidebar */
      .sidebar {
        position: fixed;
        top: 0;
        left: 0;
        height: 100vh;
        width: 260px;
        background-color: rgba(255, 255, 255, 0.02);
        backdrop-filter: blur(8px);
        border-right: 1px solid rgba(255, 255, 255, 0.1);
        padding: 2rem 0;
        overflow-y: auto;
        z-index: 1000;
      }
      
      .sidebar-header {
        padding: 0 1.5rem 2rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        margin-bottom: 1rem;
      }
      
      .sidebar-header h3 {
        color: #fafafa;
        font-weight: 600;
        margin: 0;
        font-size: 1.5rem;
        letter-spacing: -0.025em;
      }
      
      .sidebar-nav {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      
      .sidebar-nav li {
        margin: 0;
      }
      
      .sidebar-nav a {
        display: flex;
        align-items: center;
        padding: 0.875rem 1.5rem;
        color: #e5e5e5;
        text-decoration: none;
        transition: all 0.2s;
        border-left: 3px solid transparent;
      }
      
      .sidebar-nav a:hover {
        background-color: rgba(255, 255, 255, 0.05);
        color: #fafafa;
      }
      
      .sidebar-nav a.active {
        background-color: rgba(255, 255, 255, 0.08);
        color: #fafafa;
        border-left-color: rgba(255, 255, 255, 0.3);
        font-weight: 500;
      }
      
      .sidebar-nav i {
        margin-right: 0.75rem;
        width: 20px;
        font-size: 1.1rem;
      }
      
      .main-content {
        position: relative;
        margin-left: 260px;
        min-height: 100vh;
        padding: 2rem;
        z-index: 1;
      }
      
      .main-content.full-width {
        margin-left: 0;
      }
      
      .page-header {
        margin-bottom: 2rem;
      }
      
      .page-header h1 {
        font-size: 2rem;
        font-weight: 600;
        color: #fafafa;
        margin: 0 0 0.5rem;
        letter-spacing: -0.025em;
      }
      
      .page-header p {
        color: #d4d4d4;
        margin: 0;
      }
      
      /* Dark Cards */
      .card {
        position: relative;
        overflow: hidden;
        border-radius: 1rem;
        border: 1px solid rgba(255, 255, 255, 0.1);
        background-color: rgba(255, 255, 255, 0.02);
        margin-bottom: 1.5rem;
        transition: background-color 0.2s;
      }
      
      .card:hover {
        background-color: rgba(255, 255, 255, 0.04);
      }
      
      .card-glow {
        position: absolute;
        right: -2.5rem;
        top: -2.5rem;
        height: 8rem;
        width: 8rem;
        border-radius: 9999px;
        background: linear-gradient(to bottom right, rgba(255, 255, 255, 0.1), transparent);
        filter: blur(32px);
        opacity: 0.6;
      }
      
      .card-header {
        padding: 1.25rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background-color: rgba(255, 255, 255, 0.02);
        font-weight: 600;
        color: #fafafa;
      }
      
      .card-body {
        padding: 1.5rem;
        color: #e5e5e5;
      }
      
      /* Dark Forms */
      .form-label {
        display: block;
        font-size: 0.875rem;
        font-weight: 500;
        color: #e5e5e5;
        margin-bottom: 0.5rem;
      }
      
      .form-control,
      .form-select {
        width: 100%;
        padding: 0.75rem;
        border-radius: 0.5rem;
        border: 1px solid rgba(255, 255, 255, 0.1);
        background-color: rgba(255, 255, 255, 0.05);
        color: #fafafa;
        font-size: 0.875rem;
        transition: border-color 0.2s, background-color 0.2s;
      }
      
      .form-control:focus,
      .form-select:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.2);
        background-color: rgba(255, 255, 255, 0.08);
        color: #fafafa;
      }
      
      .form-control::placeholder {
        color: #a3a3a3;
      }
      
      /* Dark Buttons */
      .btn {
        border-radius: 0.5rem;
        padding: 0.5rem 1rem;
        font-weight: 500;
        font-size: 0.875rem;
        transition: all 0.2s;
        border: 1px solid transparent;
        cursor: pointer;
      }
      
      /* Ensure buttons inside forms (from button_to helper) are clickable */
      form.button_to {
        display: inline-block;
      }
      
      form.button_to button,
      form.button_to input[type="submit"] {
        cursor: pointer;
      }
      
      .btn-primary {
        background-color: white;
        color: #171717;
        font-weight: 600;
      }
      
      .btn-primary:hover {
        background-color: #e5e5e5;
        color: #171717;
      }
      
      .btn-secondary {
        background-color: rgba(255, 255, 255, 0.05);
        color: #fafafa;
        border-color: rgba(255, 255, 255, 0.2);
      }
      
      .btn-secondary:hover {
        background-color: rgba(255, 255, 255, 0.1);
        color: #fafafa;
      }
      
      .btn-outline-primary {
        background-color: transparent;
        color: #fafafa;
        border-color: rgba(255, 255, 255, 0.2);
      }
      
      .btn-outline-primary:hover {
        background-color: white;
        color: #171717;
        border-color: white;
      }
      
      .btn-outline-secondary {
        background-color: transparent;
        color: #e5e5e5;
        border-color: rgba(255, 255, 255, 0.15);
      }
      
      .btn-outline-secondary:hover {
        background-color: rgba(255, 255, 255, 0.05);
        color: #fafafa;
      }
      
      .btn-outline-danger {
        background-color: transparent;
        color: #fca5a5;
        border-color: rgba(239, 68, 68, 0.3);
      }
      
      .btn-outline-danger:hover {
        background-color: rgba(239, 68, 68, 0.1);
        color: #fca5a5;
        border-color: rgba(239, 68, 68, 0.5);
      }
      
      .btn-danger {
        background-color: rgba(239, 68, 68, 0.2);
        color: #fca5a5;
        border-color: rgba(239, 68, 68, 0.3);
      }
      
      .btn-danger:hover {
        background-color: rgba(239, 68, 68, 0.3);
        color: #fca5a5;
      }
      
      .btn-sm {
        padding: 0.375rem 0.75rem;
        font-size: 0.8125rem;
      }
      
      /* Dark Tables */
      .table {
        margin-bottom: 0;
        color: #e5e5e5;
      }
      
      .table thead th {
        border-bottom: 2px solid rgba(255, 255, 255, 0.1);
        font-weight: 600;
        color: #fafafa;
        text-transform: none;
        font-size: 0.875rem;
        letter-spacing: normal;
        padding: 0.75rem 0.5rem;
        background-color: transparent;
      }
      
      .table tbody td {
        padding: 0.75rem 0.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        color: #e5e5e5;
      }
      
      .table tbody tr:last-child td {
        border-bottom: none;
      }
      
      .table-hover tbody tr:hover {
        background-color: rgba(255, 255, 255, 0.05);
      }
      
      /* Dark Alerts */
      .alert {
        border-radius: 0.5rem;
        padding: 0.75rem 1rem;
        margin-bottom: 1rem;
        border: 1px solid;
      }
      
      .alert-success {
        background-color: rgba(16, 185, 129, 0.1);
        border-color: rgba(16, 185, 129, 0.2);
        color: #6ee7b7;
      }
      
      .alert-danger {
        background-color: rgba(239, 68, 68, 0.1);
        border-color: rgba(239, 68, 68, 0.2);
        color: #fca5a5;
      }
      
      .alert-info {
        background-color: rgba(59, 130, 246, 0.1);
        border-color: rgba(59, 130, 246, 0.2);
        color: #93c5fd;
      }
      
      /* Dark Badges */
      .badge {
        padding: 0.25rem 0.5rem;
        border-radius: 0.375rem;
        font-size: 0.75rem;
        font-weight: 500;
      }
      
      .bg-success {
        background-color: rgba(16, 185, 129, 0.2) !important;
        color: #6ee7b7 !important;
      }
      
      .bg-secondary {
        background-color: rgba(255, 255, 255, 0.1) !important;
        color: #e5e5e5 !important;
      }
      
      .bg-primary {
        background-color: rgba(255, 255, 255, 0.15) !important;
        color: #fafafa !important;
      }
      
      /* Metrics Grid */
      .metrics-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
      }
      
      @media (min-width: 768px) {
        .metrics-grid {
          grid-template-columns: repeat(4, 1fr);
        }
      }
      
      .metric-tile {
        position: relative;
        overflow: hidden;
        padding: 1rem;
        background-color: rgba(255, 255, 255, 0.02);
        border-radius: 0.75rem;
        border: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.2s ease;
      }
      
      .metric-tile:hover {
        background-color: rgba(255, 255, 255, 0.04);
        border-color: rgba(255, 255, 255, 0.15);
      }
      
      .metric-label {
        font-size: 0.75rem;
        font-weight: 500;
        color: #a3a3a3;
        margin-bottom: 0.5rem;
        text-transform: uppercase;
        letter-spacing: 0.025em;
      }
      
      .metric-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: #fafafa;
        margin-bottom: 0.25rem;
        line-height: 1.2;
      }
      
      .metric-subtext {
        font-size: 0.875rem;
        color: #d4d4d4;
        font-weight: 500;
      }
      
      /* Dark Progress Bars */
      .progress {
        height: 20px;
        background-color: rgba(255, 255, 255, 0.05);
        border-radius: 0.5rem;
        overflow: hidden;
      }
      
      .progress-bar {
        background-color: rgba(255, 255, 255, 0.2);
        color: #fafafa;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.75rem;
        font-weight: 500;
      }
      
      /* Dark Modals */
      .modal-content {
        background-color: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        color: #e5e5e5;
      }
      
      .modal-header {
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background-color: rgba(255, 255, 255, 0.02);
      }
      
      .modal-title {
        color: #fafafa;
      }
      
      .modal-body {
        color: #e5e5e5;
      }
      
      .modal-footer {
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        background-color: rgba(255, 255, 255, 0.02);
      }
      
      .btn-close {
        filter: invert(1);
        opacity: 0.7;
      }
      
      .btn-close:hover {
        opacity: 1;
      }
      
      /* Dark Text Utilities */
      .text-muted {
        color: #a3a3a3 !important;
      }
      
      .text-success {
        color: #6ee7b7 !important;
      }
      
      .text-danger {
        color: #fca5a5 !important;
      }
      
      .text-primary {
        color: #fafafa !important;
      }
      
      /* Dialog styling */
      dialog {
        padding: 0;
        border: none;
        border-radius: 1rem;
        max-width: 500px;
        width: 90%;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.1);
        margin: auto;
        overflow: visible;
      }
      
      dialog[open] {
        display: flex;
      }
      
      dialog::backdrop {
        background: rgba(0, 0, 0, 0.7);
        backdrop-filter: blur(2px);
      }
      
      dialog .modal-content {
        display: flex;
        flex-direction: column;
        border-radius: 1rem;
        overflow: hidden;
        background: rgba(255, 255, 255, 0.02);
        width: 100%;
        color: #e5e5e5;
      }
      
      dialog .modal-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(255, 255, 255, 0.02);
      }
      
      dialog .modal-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: #fafafa;
        margin: 0;
      }
      
      dialog .modal-body {
        padding: 1.5rem;
        color: #e5e5e5;
        font-size: 0.95rem;
        line-height: 1.5;
        flex: 1;
      }
      
      dialog .modal-footer {
        padding: 1rem 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        background: rgba(255, 255, 255, 0.02);
        display: flex;
        gap: 0.75rem;
        justify-content: flex-end;
      }
      
      dialog .btn-close {
        filter: invert(1);
        opacity: 0.7;
      }
      
      dialog .btn-close:hover {
        opacity: 1;
      }
      
      /* Responsive */
      @media (max-width: 768px) {
        .sidebar {
          transform: translateX(-100%);
          transition: transform 0.3s;
        }
        
        .sidebar.show {
          transform: translateX(0);
        }
        
        .main-content {
          margin-left: 0;
        }
      }
      
      /* Override Bootstrap defaults for dark theme */
      .text-dark {
        color: #fafafa !important;
      }
      
      .bg-light {
        background-color: rgba(255, 255, 255, 0.02) !important;
      }
      
      .bg-white {
        background-color: rgba(255, 255, 255, 0.02) !important;
      }
      
      .border {
        border-color: rgba(255, 255, 255, 0.1) !important;
      }
    </style>
  </head>
  
  <body>
    <!-- Backdrop gradients -->
    <div class="backdrop-gradients" aria-hidden="true">
      <div class="gradient-1"></div>
      <% 
        # Determine if we're on an auth page (landing, login, signup)
        auth_pages = ['landing', 'sessions', 'registrations']
        is_auth_page = auth_pages.include?(controller.controller_name)
      %>
      <% if is_auth_page %>
        <div class="gradient-2"></div>
      <% end %>
    </div>
    
    <!-- Grid pattern - only on landing page -->
    <% if controller.controller_name == 'landing' %>
      <div class="grid-pattern" aria-hidden="true"></div>
    <% end %>
    
    <% 
      show_sidebar = user_signed_in? && !is_auth_page
    %>
    
    <% if show_sidebar %>
      <% begin %>
        <div class="sidebar">
          <div class="sidebar-header">
            <h3><%= company_name %></h3>
            <% if user_signed_in? %>
              <div style="margin-top: 1rem; padding-top: 1rem; border-top: 1px solid rgba(255, 255, 255, 0.1);">
                <div style="font-size: 0.875rem; color: #a3a3a3; margin-bottom: 0.5rem;">
                  <%= current_user.email %>
                </div>
                <%= button_to logout_path, method: :delete, class: "btn btn-sm btn-outline-secondary", style: "width: 100%; font-size: 0.75rem;" do %>
                  <i class="bi bi-box-arrow-right"></i> Sign Out
                <% end %>
              </div>
            <% end %>
          </div>
          
          <ul class="sidebar-nav">
            <li>
              <%= link_to dashboard_index_path, class: "nav-link #{'active' if current_page?(dashboard_index_path)}" do %>
                <i class="bi bi-speedometer2"></i>
                <span>Dashboard</span>
              <% end %>
            </li>
            <li>
              <%= link_to portfolios_path, class: "nav-link #{'active' if current_page?(portfolios_path)}" do %>
                <i class="bi bi-graph-up"></i>
                <span>Portfolio</span>
              <% end %>
            </li>
            <li>
              <%= link_to savings_accounts_path, class: "nav-link #{'active' if current_page?(savings_accounts_path)}" do %>
                <i class="bi bi-piggy-bank"></i>
                <span>Savings & Expenses</span>
              <% end %>
            </li>
            <li>
              <%= link_to loans_path, class: "nav-link #{'active' if current_page?(loans_path)}" do %>
                <i class="bi bi-cash-coin"></i>
                <span>Loans</span>
              <% end %>
            </li>
            <li>
              <%= link_to insurance_policies_path, class: "nav-link #{'active' if current_page?(insurance_policies_path)}" do %>
                <i class="bi bi-shield-check"></i>
                <span>Insurance</span>
              <% end %>
            </li>
            <li>
              <%= link_to tax_scenarios_path, class: "nav-link #{'active' if current_page?(tax_scenarios_path)}" do %>
                <i class="bi bi-receipt"></i>
                <span>Taxes</span>
              <% end %>
            </li>
            <li>
              <%= link_to retirement_scenarios_path, class: "nav-link #{'active' if current_page?(retirement_scenarios_path)}" do %>
                <i class="bi bi-calendar-check"></i>
                <span>Retirement</span>
              <% end %>
            </li>
            <li>
              <%= link_to settings_path, class: "nav-link #{'active' if current_page?(settings_path)}" do %>
                <i class="bi bi-gear"></i>
                <span>Settings</span>
              <% end %>
            </li>
          </ul>
        </div>
      <% rescue => e %>
        <% # Silently handle any errors in sidebar rendering %>
      <% end %>
    <% end %>
    
    <div class="main-content <%= 'full-width' unless show_sidebar %>">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      
      <%= yield %>
    </div>
  </body>
</html>
